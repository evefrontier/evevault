[install]
# Only install package versions published at least 36 hours ago
minimumReleaseAge = 129600

[install.scopes]
"@evefrontier" = { url = "https://npm.pkg.github.com/", token = "$GITHUB_TOKEN" }

